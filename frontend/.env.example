# URL de l'API REST (backend NestJS)
VITE_API_URL=http://localhost:3000

# URL du serveur WebSocket (même backend)
VITE_WS_URL=http://localhost:3000

# true = données mockées côté front (développement sans backend)
# false = utilisation réelle de l'API
VITE_USE_MOCK=false
